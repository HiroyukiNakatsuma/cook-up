<%= @account.user_name %>さん

ご利用いただきありがとうございます！
以下のURLリンクからパスワードを再設定してください。

<%= "#{Settings.url.domain}/password_resets/#{@account.reset_token}/edit?email=#{@account.email}" %>

※上記URLでうまくいかない場合は、URLをコピーしてブラウザで表示してください。
